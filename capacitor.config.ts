import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'prepared.food.lister',
  appName: 'prepared-food-lister',
  webDir: 'www'
};

export default config;
